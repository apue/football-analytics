# 练习

## 解释

不阅读实现代码，解释：

- event、360 frame 和 freeze-frame player 三种分析单位有什么区别？
- 为什么没有 360 frame 不自动表示数据错误？
- 为什么第二半场不能按真实电视转播方向再次翻转 StatsBomb event 坐标？

## 迁移

选择另一场具有 360 的比赛，调用同一加载与质量报告接口。运行前预测其 frame
覆盖率，再解释哪些差异可能来自比赛本身，哪些可能来自数据覆盖。

## 质疑定义

将坐标合法范围从含边界改为不含边界，检查会新增哪些异常？说明为什么这是规则
变化，而不是源数据突然变差。

## 下一个问题

在通过质量检查的数据上，定义一个门将出球场景表需要哪些字段？先列出可直接观察
的字段，再列出必须通过阈值构造或无法由 360 直接观察的字段。
